# Examples

This folder can be used for small demonstration outputs only.

Do not commit large full-result folders by default. Use `.gitignore` to exclude runtime `results/` and `logs/`.

Recommended example artifacts:
- selected figures
- small demo report
- small table excerpts
